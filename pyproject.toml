[project]
name = "civiclens-ai"
version = "0.1.0"
description = "First-person multimodal and embodied intelligence platform."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
